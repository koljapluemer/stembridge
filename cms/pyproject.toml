[project]
name = "cms"
version = "0.1.0"
description = "Exercise generators for the Stembridge practice app"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.9",
]

[tool.uv]
package = false
